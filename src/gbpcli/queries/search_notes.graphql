query ($machine: String!, $key: String!) {
  searchNotes(machine: $machine, key: $key) {
    id
    machine
    keep
    published
    tags
    notes
    submitted
    completed
    packagesBuilt {
      cpv
    }
  }
}
