query ($name: String!, $key: String!) {
  searchNotes(name: $name, key: $key) {
    id
    machine
    keep
    published
    notes
    submitted
    completed
    packagesBuilt {
      cpv
    }
  }
}
