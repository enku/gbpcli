query ($machine: String!) {
  builds(machine: $machine) {
    id
    machine
    built
    submitted
    completed
    published
    tags
    notes
    keep
    packagesBuilt {
      cpv
      buildTime
    }
  }
}
