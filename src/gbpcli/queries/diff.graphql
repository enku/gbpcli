query ($left: ID!, $right: ID!) {
  diff(left: $left, right: $right) {
    left {
      id
      machine
      built
      submitted
    }
    right {
      id
      machine
      built
      submitted
    }
    items {
      item
      status
    }
  }
}
